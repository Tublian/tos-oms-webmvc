
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <context:property-placeholder location="classpath:smtp.properties" />

    <bean id="emailService" class="com.oms.service.EmailService">
        <property name="smtpHost" value="${smtp.host}" />
        <property name="smtpPort" value="${smtp.port}" />
        <!-- Add additional SMTP properties as required, matching definitions in smtp.properties -->
    </bean>

    <!-- other bean definitions can follow -->
</beans>